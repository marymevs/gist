/node_modules
/dist
.angular
/src/.DS_Store
.DS_Store

# Firebase
.firebase
*-debug.log
.runtimeconfig.json
.gstack/

# Secrets / local env (never commit) — keep .env.example tracked
.env
.env.*
!.env.example

# Misc
/scripts
.claude/
/docs

# Local planning artifacts — strategy docs that live on disk, not in git
PLANS.md
SHIP_PLAN.md
SHIP_PLAN.pdf
INTEGRATIONS_PLAN.md
